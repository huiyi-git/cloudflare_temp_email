{
  "name": "temp-email-api-proxy",
  "version": "1.0.0",
  "description": "API proxy for cloudflare temp email",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "wrangler": "^3.0.0"
  }
}
